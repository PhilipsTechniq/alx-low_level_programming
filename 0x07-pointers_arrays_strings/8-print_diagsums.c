#include "main.h"
#include <stdio.h>
/**
 * print_diagsums - the sum of the two diagonals
 * @a: array sum
 * @size: size of a sqaure
 * Return: void
 */
void print_diagsums(int *a, int size)
{
	int i, j, y;

	i = 0;
	j = 0;

	for (y = 0; y < size; y++)
	{
		i = i + j;
	}
	return (0);
}
